# URL бэкенда (пусто = тот же домен, нужно только для разработки если сервер на другом порту)
# Для production на Vercel оставь пустым если API там же
# Для Railway/Render: VITE_API_URL=https://your-server.railway.app
VITE_API_URL=
